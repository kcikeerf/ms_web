:concurrency: 10
:pidfile: ./tmp/pids/sidekiq.pid
:logfile: ./log/sidekiq.log

:queues:
    - default
    - [report, 2]
    - [result, 2]
    - [score. 2]

development:
  :concurrency: 5
staging:
  :concurrency: 10
production:
  :concurrency: 20
