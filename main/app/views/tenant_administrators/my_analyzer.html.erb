<% @page_title = t('page.user_left_menu.my_home') %>

<section>
	<div class="container clearfix">
		<!-- Nav tabs -->
		<%= render 'shared/user/left_menu' %>

		<!-- Tab panes -->
		<div class="tab-content">
			<div role="tabpanel" id="test">

				<div class="panel">

					  <!-- 我的分析员 -->
					  <h1 class="panel-heading clearfix">
                        <div class="col-lg-4 input-group pull-right">
                            <%= text_field_tag :keyword, params[:keyword], class: 'form-control', placeholder: '请输入分析员名字搜索' %>
                            <span class="input-group-btn" id='keyword_filter'>
                                <button class="btn btn-default" type="button"><i class="fa fa-search search"></i></button>
                            </span>
                        </div>
						  <%= I18n.t("page.user_left_menu.my_analyzer")%>
                      </h1>
					  <div class="panel-body">
                            
                            <table id="testtable" class="table table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th><%=I18n.t("activerecord.attributes.user.name")%></th>
                                        <th><%=I18n.t("activerecord.attributes.analyzer.name")%></th>
                                        <th><%=I18n.t("activerecord.attributes.analyzer.subject")%></th>
                                        <th><%=I18n.t("activerecord.attributes.user.email")%></th>
                                    </tr>
								</thead>
								<tbody>
<% @analyzers.each do |analyzer| %>
                                    <tr>
                                    	<td><%= analyzer[:user_name] %></td>
                                    	<td><%= analyzer["name"] %></td>
                                    	<td><%= I18n.t("dict.#{analyzer["subject"]}")%></td>
                                    	<td><%= analyzer[:email] %></td>
                                    </tr>
<% end %>
								</tbody>
                            </table>

						  <nav class="text-center">
							  <%= paginate @analyzers %>
						  </nav>
					  </div>

					 </div>


		</div>

	</div>

</div>
</section>