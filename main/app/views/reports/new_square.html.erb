<div class="zy-report-container">
  <!--诊断报告头部 end-->
  <div class="zy-report-topNav">
    <div class="zy-report-nav-container">
      <a class="zy-report-navEntry">
        <span>查看报告</span>
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
      </a>
      <ul class="zy-grade-menu">
        <li>
          <a
          href="#"
          report_id="<%= @scope_menus[:report_id] %>"
          report_name="<%= @scope_menus[:report_name] %>"
          data_type="<%= @scope_menus[:data_type] %>"
          >
            <span>点击查看年级报告</span>
            <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
          </a>
          <ul class="zy-class-menu">
            <% @scope_menus[:items].each do |klass| %>
                <li>
                  <a
                  report_id="<%= klass[:report_id] %>"
                  report_name="<%= klass[:report_name] %>"
                  data_type="<%= klass[:data_type] %>"
                  grade_report_id="<%= @scope_menus[:report_id] %>"
                  >
                    <span><%= klass[:label] %></span>
                    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                  </a>
                  <ul class="zy-student-menu">
                    <div class="title"><%= I18n.t("dict.ge_ren_bao_gao") %></div>
                    <% klass[:items].each do |pupil| %>
                        <li>
                          <a
                          report_id="<%= pupil[:report_id] %>"
                          report_name="<%= pupil[:report_name] %>"
                          data_type="<%= pupil[:data_type] %>"
                          class_report_id="<%= klass[:report_id] %>"
                          grade_report_id="<%= @scope_menus[:report_id] %>"
                          >
                            <%= pupil[:label] %>
                          </a>
                        </li>
                    <% end %>
                  </ul>
            <% end %>
            </li>
          </ul>
        </li>
      </ul>
    </div>
    <div class="zy-breadcrumb-container"></div>
  </div>
  <div id="reportContent" class="clearfix report-content"></div>
</div>
<script type="text/javascript">
  var ReportMenus = <%=raw @scope_menus.to_json%>;
</script>

